dependencies:
  - jupyter
  - matplotlib
  - numpy
  - psycopg2
  - pylint
  - pip
  - pip:
      # Scrapling: HTML parsing plus HTTP/browser fetchers. See docs/scrapling-setup.md.
      - "scrapling[fetchers]"
